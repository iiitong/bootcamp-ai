# DB Query Tool Backend - Environment Variables
# Copy this file to .env and modify as needed

# =============================================================================
# OpenAI API Configuration (for Natural Language to SQL feature)
# =============================================================================

# OpenAI API Key (required for NL to SQL feature)
# OPENAI_API_KEY=sk-your-api-key-here

# Custom API endpoint (optional, for OpenAI-compatible APIs like Azure, Bedrock)
# OPENAI_BASE_URL=https://api.openai.com/v1

# Model to use (default: gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini

# =============================================================================
# Storage Configuration
# =============================================================================

# Directory for SQLite database storage (default: ~/.db_query)
# DB_QUERY_DATA_DIR=/path/to/data

# =============================================================================
# Query Settings
# =============================================================================

# Default LIMIT for queries without explicit LIMIT (default: 1000)
# DB_QUERY_DEFAULT_LIMIT=1000

# Query timeout in seconds (default: 30)
# DB_QUERY_TIMEOUT=30

# =============================================================================
# CORS Configuration
# =============================================================================

# Comma-separated list of allowed origins
# Default: http://localhost:5173,http://localhost:5174 (Vite dev servers)
# For production, set to your actual frontend domain(s)
# CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174

# Example for production:
# CORS_ALLOWED_ORIGINS=https://your-frontend-domain.com
