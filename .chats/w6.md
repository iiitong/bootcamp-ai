# instructions

## simple agent 构建

基于 @specs/w6/0001-simple-agent-design.md 的规范，使用 openai 构建一个 agent sdk，提供 agent 的核心功能，用户可以很方便的为 agent 添加自定义工具和 mcp。完成构建后，确保所有实现都符合 design spec，并提供几个 example 来展如何使用（包含至少一个使用 mcp 的例子）。

所有的 deps 要使用最新的 version。
